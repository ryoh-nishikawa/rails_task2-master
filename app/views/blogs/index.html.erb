<div class="index-msg">
  <p>「TRY !!」をクリックすればいいだけ🎶<p>
</div>

<h1>T新規ブログを作成する</h1>

<div id="new-blog-link">
  <%= link_to 'TRY !!', new_blog_path %>
</div>

<% @blogs.each do |blog| %>
  <div class="blog-panel">
    <h2><%= blog.title %></h2>
    <p><%= blog.content %></p>
    <div class="blog-links">
      <%= link_to 'Show', blog %>
      <%= link_to 'Edit', edit_blog_path(blog) %> |
      <%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  </div>
<% end %>
